# コマンドリファレンス

{{PROJECT_NAME}} でよく使うコマンドをまとめます。

<!-- 
📝 書くべき内容:
- 開発時によく使うコマンド
- テスト関連コマンド
- ビルド・デプロイコマンド
- トラブルシューティング用コマンド

カスタマイズ:
- プロジェクトで使用するコマンドに合わせて更新
-->

## 📚 目次

1. [開発](#開発)
2. [テスト](#テスト)
3. [Lint・型チェック](#lint型チェック)
4. [ビルド・デプロイ](#ビルドデプロイ)
5. [データベース](#データベース)
6. [トラブルシューティング](#トラブルシューティング)

---

## 開発

### 開発サーバー起動

```bash
# 通常起動
npm run dev

# カスタムポート
PORT=8080 npm run dev

# モックモード（APIモック使用）
npm run dev:mock
```

### 依存関係

```bash
# インストール
npm install

# パッケージ追加
npm install <package-name>

# 開発用パッケージ追加
npm install -D <package-name>

# パッケージ更新
npm update

# 古いパッケージ確認
npm outdated
```

---

## テスト

### ユニットテスト

```bash
# 実行
npm run test

# ウォッチモード
npm run test:watch

# カバレッジ付き
npm run test:coverage

# 特定ファイルのみ
npm run test -- src/components/Button.spec.tsx

# 特定パターンのみ
npm run test -- --grep "should render"
```

### E2Eテスト

```bash
# ヘッドレス実行
npm run test:e2e

# UIモード（デバッグ用）
npm run test:e2e:ui

# 特定ファイルのみ
npx playwright test tests/e2e/login.spec.ts

# 特定ブラウザのみ
npx playwright test --project=chromium

# デバッグモード
npx playwright test --debug
```

---

## Lint・型チェック

### ESLint

```bash
# Lint実行
npm run lint

# 自動修正
npm run lint:fix
```

### TypeScript

```bash
# 型チェック
npx tsc --noEmit

# 型チェック（ウォッチモード）
npx tsc --noEmit --watch
```

### 品質チェック一括実行

```bash
# コード編集後は必ず実行
npm run lint && npx tsc --noEmit && npm run test
```

---

## ビルド・デプロイ

### ビルド

```bash
# 本番ビルド
npm run build

# ビルド結果をローカルで確認
npm run start
```

### デプロイ

```bash
# Vercel（自動デプロイの場合）
git push origin main

# 手動デプロイ
npx vercel
```

---

## データベース

<!-- Supabase/Prisma等、プロジェクトに合わせて更新 -->

### マイグレーション

```bash
# マイグレーション作成
npx supabase migration new <migration_name>

# マイグレーション実行
npx supabase db push

# マイグレーション状態確認
npx supabase migration list

# ローカルDBリセット
npx supabase db reset
```

### Supabase

```bash
# ローカル起動
npx supabase start

# ローカル停止
npx supabase stop

# スタジオ（管理画面）
# http://localhost:54323

# 型生成
npx supabase gen types typescript --local > src/types/database.ts
```

---

## トラブルシューティング

### キャッシュクリア

```bash
# Next.js キャッシュクリア
rm -rf .next

# node_modules 再インストール
rm -rf node_modules package-lock.json
npm install
```

### ポート競合

```bash
# 使用中のポートを確認
lsof -i :3000

# プロセス終了
kill -9 <PID>
```

### セキュリティ

```bash
# 脆弱性チェック
npm audit

# 自動修正
npm audit fix

# 強制修正（破壊的変更を含む可能性）
npm audit fix --force
```

---

## よく使うコマンド一覧

| カテゴリ | コマンド | 説明 |
|---------|---------|------|
| 開発 | `npm run dev` | 開発サーバー起動 |
| テスト | `npm run test` | ユニットテスト |
| テスト | `npm run test:e2e` | E2Eテスト |
| Lint | `npm run lint` | ESLint実行 |
| 型 | `npx tsc --noEmit` | 型チェック |
| ビルド | `npm run build` | 本番ビルド |
| DB | `npx supabase start` | ローカルDB起動 |

---

## 関連ドキュメント

- [../guides/80-testing.md](../guides/80-testing.md) - テスト戦略
- [../guides/90-development-workflow.md](../guides/90-development-workflow.md) - 開発ワークフロー

---

**最終更新**: YYYY-MM-DD

